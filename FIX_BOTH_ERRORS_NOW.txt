================================================================================
                    FIX BOTH ERRORS - STEP BY STEP
================================================================================

ERROR 1: "must be owner of table objects"
  → Caused by trying to modify storage.objects permissions
  ✅ FIXED: Use migration 097 instead (doesn't touch storage)

ERROR 2: "column created_by does not exist"
  → Caused by old code trying to use created_by column
  ✅ FIXED: Code already uses teacher_id instead

================================================================================
                       STEP 1: CREATE BUCKETS
================================================================================

RUN MIGRATION 097 (SAFE VERSION):

File: database/migrations/097_safe_storage_buckets.sql

Steps:
1. Go to Supabase SQL Editor
2. Create new query
3. Copy-paste ENTIRE content of 097_safe_storage_buckets.sql
4. Click Run
5. Done! All 3 buckets created ✅

Expected Output:
  ✅ "Query successful"
  No errors, no permission issues

What it does:
  ✅ Creates lesson-uploads bucket
  ✅ Creates assignment-files bucket
  ✅ Creates student-assignments bucket
  ✅ NO RLS modifications (safe!)
  ✅ NO storage.objects modifications (safe!)

================================================================================
                    STEP 2: DISABLE RLS IN SUPABASE UI
================================================================================

After buckets are created:

1. Go to Supabase Dashboard
2. Click: Storage
3. Click: Policies tab
4. For EACH bucket:
   - Select bucket name
   - Delete all existing policies
   - Toggle RLS OFF
5. Done!

This allows uploads without auth issues.

================================================================================
                   STEP 3: FIX DATABASE (OPTIONAL)
================================================================================

The "created_by" error shouldn't appear because code uses teacher_id.

If you still see it:

Run Migration 095 (already created):
  File: database/migrations/095_minimal_schema_fix.sql
  
This adds teacher_id column if missing and removes created_by references.

================================================================================
                        STEP 4: RESTART & TEST
================================================================================

After migrations:

1. Restart dev server:
   Kill: Ctrl+C
   Run: npm run dev

2. Test Lesson Note Upload:
   Go to: /teacher/lesson-notes
   Create lesson note
   Select file to upload
   Submit → Should work ✅

3. Test Assignment Upload:
   Go to: /student/assignments
   View assignment
   Upload file → Should work ✅

================================================================================
                      QUICK ACTION SUMMARY
================================================================================

DO THIS NOW:

1. ❌ DELETE migration 096 (has permission errors)
   ✅ DONE - Already deleted

2. ✅ RUN migration 097 (safe bucket creation)
   - File: database/migrations/097_safe_storage_buckets.sql
   - Copy-paste into Supabase SQL Editor
   - Click Run

3. ✅ DISABLE RLS IN SUPABASE UI
   - Storage → Policies
   - For each bucket: Delete policies, turn off RLS
   - (Or do this manually in Supabase dashboard)

4. ✅ RESTART DEV SERVER
   - Ctrl+C to stop
   - npm run dev to restart

5. ✅ TEST UPLOADS
   - Try lesson note upload
   - Try assignment upload

================================================================================
                        MIGRATION 097 CONTENT
================================================================================

Simple and safe - just creates buckets:

BEGIN;

INSERT INTO storage.buckets (id, name, owner, public, created_at, updated_at)
VALUES 
  ('lesson-uploads', 'lesson-uploads', NULL, true, NOW(), NOW())
ON CONFLICT DO NOTHING;

INSERT INTO storage.buckets (id, name, owner, public, created_at, updated_at)
VALUES 
  ('assignment-files', 'assignment-files', NULL, true, NOW(), NOW())
ON CONFLICT DO NOTHING;

INSERT INTO storage.buckets (id, name, owner, public, created_at, updated_at)
VALUES 
  ('student-assignments', 'student-assignments', NULL, true, NOW(), NOW())
ON CONFLICT DO NOTHING;

COMMIT;

That's it! No RLS changes, no permission issues, just buckets.

================================================================================
                          VERIFICATION
================================================================================

After running migration 097:

1. Go to Supabase Storage
2. Should see 3 buckets:
   ✅ lesson-uploads
   ✅ assignment-files
   ✅ student-assignments

If you see them → Migration worked! ✅

================================================================================
                    THEN DISABLE RLS (MANUAL STEP)
================================================================================

In Supabase Dashboard:

1. Storage tab → Policies
2. Select "lesson-uploads" bucket
3. Delete all policies
4. Toggle RLS OFF

Repeat for:
  - assignment-files
  - student-assignments

This allows all uploads without authentication blocking.

================================================================================
                            DONE ✅
================================================================================

After these steps:

✅ Buckets created
✅ RLS disabled
✅ Teachers can upload lesson notes
✅ Students can upload assignments
✅ No permission errors
✅ No "created_by" errors

Ready to use! 🚀

================================================================================
                        IF STILL PROBLEMS
================================================================================

If upload still fails:

1. Restart dev server (Ctrl+C, npm run dev)
2. Check browser console (F12) for exact error
3. Go to Supabase → Storage → Check buckets exist
4. Go to Supabase → Storage → Policies → Verify RLS is OFF

If still failing:
  - Manual bucket creation via Supabase UI
  - Make sure RLS is disabled
  - Restart dev server again
  - Try upload again

================================================================================
